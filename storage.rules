rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // Property images — any authenticated user may upload, but only real images
    // up to 10MB. (Object names are timestamp+random, so accidental clobbering
    // of someone else's upload is effectively impossible.)
    match /properties/{fileName} {
      allow read: if true;
      allow create, update: if isAuthenticated()
                   && request.resource.size < 10 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
      allow delete: if isAuthenticated();
    }

    // Match user documents
    match /user_documents/{userId}/{fileName} {
      // Only the owner can read or write their own documents
      allow read: if isOwner(userId);
      allow create, update: if isOwner(userId)
                   // Limit file size to 5MB
                   && request.resource.size < 5 * 1024 * 1024
                   // Only allow specific content types (PDFs, Images)
                   && request.resource.contentType.matches('application/pdf|image/.*');
      allow delete: if isOwner(userId);
    }
    
    // Default deny
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
